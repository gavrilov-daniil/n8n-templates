# Подключение Битрикс24 к ИИ Бизнес-аналитику

Пошаговая инструкция по настройке интеграции.

---

## Шаг 1: Подготовка n8n

### 1.1 Установка n8n (если ещё не установлен)

**Docker (рекомендуется):**
```bash
docker run -d \
  --name n8n \
  -p 5678:5678 \
  -v n8n_data:/home/node/.n8n \
  -e N8N_SECURE_COOKIE=false \
  n8nio/n8n
```

**npm:**
```bash
npm install -g n8n
n8n start
```

### 1.2 Открыть n8n

Перейдите в браузере: `http://localhost:5678`

При первом запуске создайте учётную запись администратора.

---

## Шаг 2: Настройка Битрикс24

### 2.1 Создание входящего вебхука (REST API)

Входящий вебхук позволяет n8n вызывать API Битрикс24.

1. Откройте Битрикс24
2. Перейдите: **Приложения** → **Разработчикам** → **Другое** → **Входящий вебхук**
3. Нажмите **Добавить вебхук**
4. Выберите права доступа:

| Право | Описание |
|-------|----------|
| ☑️ `task` | Задачи (чтение и запись) |
| ☑️ `user` | Пользователи (чтение) |
| ☑️ `im` | Сообщения и уведомления |

5. Нажмите **Сохранить**
6. **Скопируйте URL вебхука**, он выглядит так:
```
https://ваш-домен.bitrix24.ru/rest/1/xxxxxxxxxxxxxxxxxx/
```

⚠️ **Важно:** Сохраните этот URL, он понадобится для настройки n8n.

---

### 2.2 Настройка исходящих вебхуков (события → n8n)

Исходящие вебхуки отправляют события из Битрикс24 в n8n при создании задач и комментариев.

#### Путь в Битрикс24:
```
Приложения → Разработчикам → Готовые сценарии → Исходящий вебхук
```

Или:
```
Приложения → Вебхуки → Исходящий вебхук
```

---

#### 2.2.1 Вебхук для новых задач (ONTASKADD)

1. Нажмите **"Добавить исходящий вебхук"** или **"Создать"**

2. Заполните форму:

| Поле | Значение |
|------|----------|
| **Название** | `ИИ БА: Новая задача` |
| **URL обработчика** | URL из n8n (см. ниже) |
| **Тип события** | Выберите из списка: **Добавление задачи (ONTASKADD)** |

3. **Получение URL из n8n:**
   - Откройте workflow в n8n
   - Кликните на ноду **"Вебхук: Новая задача"**
   - Справа в панели скопируйте **Production URL**:
   ```
   https://ваш-n8n-домен.com/webhook/bitrix-tasks
   ```

4. Нажмите **Сохранить**

---

#### 2.2.2 Вебхук для комментариев (ONTASKCOMMENTADD)

1. Нажмите **"Добавить исходящий вебхук"**

2. Заполните форму:

| Поле | Значение |
|------|----------|
| **Название** | `ИИ БА: Новый комментарий` |
| **URL обработчика** | URL из n8n для комментариев |
| **Тип события** | **Добавление комментария к задаче (ONTASKCOMMENTADD)** |

3. **URL из n8n** (нода "Вебхук: Новый комментарий"):
   ```
   https://ваш-n8n-домен.com/webhook/bitrix-comments
   ```

4. Нажмите **Сохранить**

---

#### 2.2.3 Список событий для настройки

| Событие | Код | Описание | Обязательно |
|---------|-----|----------|-------------|
| Добавление задачи | `ONTASKADD` | Создана новая задача | ✅ Да |
| Добавление комментария | `ONTASKCOMMENTADD` | Новый комментарий в задаче | ✅ Да |
| Обновление задачи | `ONTASKUPDATE` | Задача изменена | ⚪ Опционально |

---

#### 2.2.4 Формат данных от Битрикс24

При срабатывании вебхука Битрикс24 отправляет POST-запрос с данными:

**Событие ONTASKADD:**
```json
{
  "event": "ONTASKADD",
  "data": {
    "FIELDS_BEFORE": {},
    "FIELDS_AFTER": {
      "ID": "123",
      "TITLE": "Название задачи",
      "DESCRIPTION": "Описание",
      "PRIORITY": "2",
      "CREATED_BY": "1",
      "RESPONSIBLE_ID": "5"
    }
  },
  "ts": "1234567890",
  "auth": {
    "domain": "ваш-домен.bitrix24.ru",
    "client_endpoint": "https://ваш-домен.bitrix24.ru/rest/",
    "member_id": "xxx"
  }
}
```

**Событие ONTASKCOMMENTADD:**
```json
{
  "event": "ONTASKCOMMENTADD",
  "data": {
    "FIELDS_BEFORE": {},
    "FIELDS_AFTER": {
      "ID": "456",
      "TASK_ID": "123",
      "POST_MESSAGE": "Текст комментария",
      "AUTHOR_ID": "1"
    }
  },
  "ts": "1234567890",
  "auth": {
    "domain": "ваш-домен.bitrix24.ru"
  }
}
```

---

#### 2.2.5 Проверка работы вебхуков

После создания вебхуков проверьте их работу:

1. **В Битрикс24** создайте тестовую задачу
2. **В n8n** откройте **Executions** (История выполнений)
3. Должен появиться новый запуск с данными от Битрикс24

Если запуска нет:
- Проверьте, что workflow активирован (зелёный тумблер)
- Проверьте правильность URL в настройках вебхука
- Убедитесь, что n8n доступен по HTTPS

---

#### 2.2.6 Частые проблемы с исходящими вебхуками

| Проблема | Причина | Решение |
|----------|---------|---------|
| Вебхук не срабатывает | Workflow не активен | Включите workflow в n8n |
| Ошибка "URL недоступен" | n8n недоступен извне | Настройте HTTPS (nginx, cloudflare, ngrok) |
| Событие не приходит | Неверный тип события | Проверьте выбранное событие |
| Нет данных о задаче | ID не передаётся | Используйте `FIELDS_AFTER.ID` |

### 2.3 Получение ID пользователя бота

Если вы создаёте отдельного пользователя для бота:

1. Перейдите: **Настройки** → **Сотрудники** → **Добавить**
2. Создайте пользователя "ИИ Бизнес-аналитик"
3. Запомните его ID (видно в URL профиля: `/company/personal/user/123/`)

Или используйте ID существующего пользователя.

---

## Шаг 3: Настройка OpenAI API

### 3.1 Получение API ключа

1. Перейдите на [platform.openai.com](https://platform.openai.com)
2. Войдите или зарегистрируйтесь
3. Перейдите: **API Keys** → **Create new secret key**
4. Скопируйте ключ (начинается с `sk-...`)

⚠️ **Важно:** Ключ показывается только один раз!

### 3.2 Проверка баланса

Убедитесь, что на аккаунте OpenAI есть средства:
- **Billing** → **Add payment method**
- Рекомендуемый минимум: $10

---

## Шаг 4: Импорт workflow в n8n

### 4.1 Импорт файла

1. Откройте n8n: `http://localhost:5678`
2. Нажмите **Add workflow** (или `Ctrl+N`)
3. Нажмите **⋮** (три точки) → **Import from File**
4. Выберите файл: `ai-ba-bot/workflows/ai-ba-main.json`
5. Workflow появится на холсте

### 4.2 Настройка переменных окружения

В n8n создайте переменные:

1. Перейдите: **Settings** → **Variables**
2. Добавьте переменные:

| Переменная | Значение | Пример |
|------------|----------|--------|
| `BITRIX24_WEBHOOK_URL` | URL входящего вебхука | `https://xxx.bitrix24.ru/rest/1/abc123/` |
| `BOT_USER_ID` | ID пользователя бота | `123` |

### 4.3 Настройка Credentials

#### OpenAI:
1. Перейдите: **Settings** → **Credentials** → **Add Credential**
2. Выберите **OpenAI API**
3. Заполните:
   - **Credential Name:** `OpenAI`
   - **API Key:** ваш ключ `sk-...`
4. Нажмите **Save**

#### Привязка к нодам:
1. Откройте workflow
2. Для каждой ноды `ИИ: ...`:
   - Дважды кликните на ноду
   - В поле **Credential** выберите `OpenAI`
   - Нажмите **Save**

---

## Шаг 5: Настройка URL вебхуков

### 5.1 Получение URL из n8n

1. Откройте workflow
2. Кликните на ноду **"Вебхук: Новая задача"**
3. Скопируйте **Webhook URL** (справа в панели)
   - Production: `https://ваш-n8n.com/webhook/bitrix-tasks`
   - Test: `https://ваш-n8n.com/webhook-test/bitrix-tasks`

4. Повторите для ноды **"Вебхук: Новый комментарий"**
   - URL: `https://ваш-n8n.com/webhook/bitrix-comments`

### 5.2 Обновление URL в Битрикс24

Вернитесь в Битрикс24 и обновите URL в исходящих вебхуках (Шаг 2.2).

---

## Шаг 6: Активация и тест

### 6.1 Активация workflow

1. В n8n откройте workflow
2. Переключите тумблер **Active** в правом верхнем углу
3. Статус должен стать зелёным

### 6.2 Тестирование

1. В Битрикс24 создайте тестовую задачу:
   ```
   Заголовок: Тестовая задача для бота
   Описание: Нужно что-то сделать
   Исполнитель: [выберите себя или бота]
   ```

2. Подождите 5-10 секунд

3. Проверьте в n8n:
   - **Executions** → должен появиться новый запуск
   - Статус: зелёный ✓ (успех) или красный ✗ (ошибка)

4. Проверьте в Битрикс24:
   - В задаче должен появиться комментарий с вопросами
   - Должен быть установлен тег `ba_pending`

---

## Шаг 7: Продакшен настройки

### 7.1 HTTPS для n8n

Битрикс24 требует HTTPS для вебхуков. Варианты:

**A) Reverse proxy (Nginx):**
```nginx
server {
    listen 443 ssl;
    server_name n8n.ваш-домен.ru;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location / {
        proxy_pass http://localhost:5678;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }
}
```

**B) Cloudflare Tunnel:**
```bash
cloudflared tunnel --url http://localhost:5678
```

**C) ngrok (для тестов):**
```bash
ngrok http 5678
```

### 7.2 Переменные окружения для Docker

```yaml
# docker-compose.yml
version: '3'
services:
  n8n:
    image: n8nio/n8n
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=сильный_пароль
      - WEBHOOK_URL=https://n8n.ваш-домен.ru/
      - BITRIX24_WEBHOOK_URL=https://xxx.bitrix24.ru/rest/1/abc123/
      - BOT_USER_ID=123
    volumes:
      - n8n_data:/home/node/.n8n
```

---

## Проверочный чек-лист

| # | Пункт | Статус |
|---|-------|--------|
| 1 | n8n установлен и доступен | ☐ |
| 2 | Входящий вебхук Битрикс24 создан | ☐ |
| 3 | Исходящий вебхук ONTASKADD настроен | ☐ |
| 4 | Исходящий вебхук ONTASKCOMMENTADD настроен | ☐ |
| 5 | OpenAI API ключ получен | ☐ |
| 6 | Workflow импортирован в n8n | ☐ |
| 7 | Переменная BITRIX24_WEBHOOK_URL задана | ☐ |
| 8 | Credential OpenAI привязан к нодам | ☐ |
| 9 | Workflow активирован | ☐ |
| 10 | Тестовая задача обработана успешно | ☐ |

---

## Возможные ошибки

### "401 Unauthorized" от Битрикс24
- Проверьте правильность URL вебхука
- Убедитесь, что вебхук не истёк
- Проверьте права доступа вебхука

### "Connection refused" в n8n
- Проверьте, что n8n запущен
- Проверьте URL в исходящем вебхуке Битрикс24
- Убедитесь, что n8n доступен извне (HTTPS)

### "Invalid API Key" от OpenAI
- Проверьте правильность ключа
- Убедитесь, что ключ активен
- Проверьте баланс аккаунта OpenAI

### Вебхук не срабатывает
- Убедитесь, что workflow активирован
- Проверьте URL в настройках Битрикс24
- Посмотрите логи в n8n (Executions)

---

## Следующие шаги

После успешного подключения:

1. **Настройте фильтрацию задач** - см. `config/task-filter.md`
2. **Изучите теги** - см. `config/tags.md`
3. **Ознакомьтесь с кейсом** - см. `examples/use-case-scenario.md`
